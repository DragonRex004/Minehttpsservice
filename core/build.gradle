plugins {
    id 'java'
    id 'com.github.johnrengelman.shadow' version '7.1.2'
    id 'application'
}

group 'de.dragonrex.core'
version '1.0.0'

repositories {
    mavenCentral()
}

dependencies {
    implementation(project(":api"))
}

tasks {
    build {
        dependsOn(shadowJar)
    }
}